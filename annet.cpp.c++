#include <iostream>
//cstdlib for random og randint fantastisk for og lage stein saks pappir bot
#include <cstdlib>
using namespace std;
//double auto nye veriabler
int main() {
    int length,hight;
    cout<<"hei skrive in hooyden for en firekant\n";
    cin>> hight;
    cout<<"hei skrive in lengden for en firekant\n";
    cin>> length;
    //++ er plus 1 --minus 1 så du trenger ikke og skrive length-1 heller --length
    cout<<"hei det er areale\n";
    cout<< length*hight;
  return 0;
}
//koden for og kjøre
/*  cd "C:\Users\JonasMikkelsenRasmus\Downloads\flow\.vscode"
g++ "annet.cpp.c++" -o annet.exe
./annet.exe  */